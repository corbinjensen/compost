/*
window.addEvent('domready', function(){
  var form = $$('.apply-form').pick();

  form.addEvent('submit', function() {
    var email = this['email'];
    var name = this['name'];
    var location = this['location'];

    debugger;
  });
});
*/
